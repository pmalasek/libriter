{
  "name": "libriter",
  "private": true,
  "workspaces": [
    "libriter-shared",
    "libriter-frontend",
    "libriter-mobile"
  ],
  "scripts": {
    "postinstall": "node _scripts/restore-expo-sqlite-sources.mjs && node _scripts/patch-track-player-android.mjs",
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}
